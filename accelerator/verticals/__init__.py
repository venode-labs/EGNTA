"""Vertical packs: the domain semantic layer that ships with EGNTA.

A vertical pins the canonical activity vocabulary, the synonym map a connector
normalises raw source statuses through, and the domain defect detectors. Shipping
the semantic layer with the product is what lets discovery run on a real export
without a bespoke per-client modelling phase first.
"""
